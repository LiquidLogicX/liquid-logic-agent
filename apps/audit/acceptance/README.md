# Acceptance artifacts

Store real Base payment + paid audit call evidence here:

- `paid-audit-*.json` from `npm run paid-call`
- BaseScan tx URLs for the $0.05 USDC settlement
- Optional screenshots

Blocked until CDP keys + USDC funding exist (see root `BLOCKERS.md`).
