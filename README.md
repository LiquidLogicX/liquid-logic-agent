# Liquid Logic Agent

Base mainnet treasurer for fleet operating spend (USDC / x402 only).

Hard guardrails: spends USDC on services only; never buys or holds any token other than USDC; no treasury-growth framing in code, logs, or copy.

Repo: `LiquidLogicX/liquid-logic-agent`. Separate from Sepolia FHE contracts (`liquid-logic-x`).
